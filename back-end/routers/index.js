const authRouter = require("./authRouters")
const productRouter = require("./productRouters")
const categoryRouter = require("./categoryRouters")

module.exports = {
    authRouter, productRouter, categoryRouter
}